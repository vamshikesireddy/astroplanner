# Python cache and virtual environments
__pycache__/
*.pyc
venv/
.venv/
env/

# Streamlit secrets (NEVER commit this)
.streamlit/secrets.toml

# Local data files (if you download them)
*.csv
pending_requests.txt
comet_pending_requests.txt
asteroid_pending_requests.txt

# Temporary files produced by CI scripts (never committed)
_new_comets.json
_priority_changes.json

# OS generated files
.DS_Store
Thumbs.db
*.pyo
*.pyd

test/*

# Temporary investigation files
temp/

# Claude Code local state (not needed in repo)
.claude/
CLAUDE.md

# Internal planning / design docs (not for public repo)
docs/plans/

# Playwright MCP browser session files
.playwright-mcp/

# Internal planning / design docs (not for public repo)
docs/plans/

# Environment variable files (may contain API keys)
.env
.env.*
*.env

# JPL ID cache — committed; updated by scripts/populate_jpl_cache.py
# jpl_id_cache.json